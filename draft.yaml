models:
  Article:
    title: string
    slug: string
    description: string
    body: string
    keywords: string
    category_id: unsignedInteger
    relationships:
      hasOne: Category
  
  Category:
    category: string
    relationships:
      belongsTo: Article

  Subscriber:
    email: string
